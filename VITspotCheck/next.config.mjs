/** @type {import('next').NextConfig} */
const nextConfig = {
  // We removed output: 'export' so your API routes can run.
  // We removed basePath so your Vercel URL is clean (no subfolders).
  // We removed unoptimized images so Vercel can optimize them for you.
};

export default nextConfig;
